# Bug #055: TypeScript async runtime lacks native socket integration — Closed

Status: Closed (Superseded by V3 roadmap)
Date: 2025-11-12

Summary
- V3 accepts native TypeScript; remaining needs (FID generation from TS imports, Node mapping, and action codegen) belong to the roadmap, not the bug tracker.

Action
- Close as Superseded. Add “FID/Native Import Mapping (TS/Py first)” to the roadmap and plan TS codegen that consumes FIDs and emits real imports/usage.

