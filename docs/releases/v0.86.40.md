# Frame Transpiler v0.86.40

- Fixes Bug #071 for Python V3:
  - Async actions and operations now emit as `async def` with correct indentation for `try/except/finally` bodies.
  - Runtime protocol harness and minimal async/try/except fixtures compile and import cleanly as Python modules.
- Clarifies bug process around V3 grammar compliance in `docs/bugs/BUG_TRACKING_POLICY.md`.

