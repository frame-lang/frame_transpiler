# Frame Transpiler v0.86.41

- Fixes Bug #072:
  - Python V3 emitter now emits public action wrappers (e.g., `def log(self, ...)`) that delegate to internal `_action_log(...)`, so FRM-level calls like `self.log("...")` and `self.handle()` work without manual rewrites.

