# Frame Transpiler v0.86.47

- Completes the V3 outer‑pipeline refactor for Python and TypeScript:
  - System/block structure, machine/state headers, handler placement, system parameters, and domain semantics are now driven by dedicated scanners/parsers and a `ModuleAst` + `Arcanum` symbol table instead of ad‑hoc byte scans.
  - `SystemParserV3`, `MachineParserV3`, and `DomainBlockScannerV3` are the authoritative sources of system outline, state headers, and domain declarations.
- Aligns the V3 documentation set with the new architecture:
  - `architecture_v3/grammar.md` describes the outer AST and its role in validation.
  - `architecture_v3/frame_language_guide.md` documents the enforced system parameter semantics (E416–E418), block ordering (E113/E114), and handler placement (E404) in terms of `ModuleAst` + `Arcanum`.
  - `architecture_v3/frame_runtime.md` remains the runtime semantic reference; the Python V3 runtime is confirmed to follow the documented system/state parameter behavior via TrafficLight and related fixtures.
  - `framelang_design/capability_matrix.md` marks system parameters and handler placement as V3‑native features with explicit PT fixtures.
- Keeps the PT V3 suites fully green:
  - `cargo build --release` completes successfully.
  - `python3 framec_tests/runner/frame_test_runner.py --languages python typescript --categories all_v3 --framec ./target/release/framec --transpile-only --include-common` → 112/112 Python, 101/101 TypeScript passing in the V3 pipeline.

