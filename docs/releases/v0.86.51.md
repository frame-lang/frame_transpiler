# Frame Transpiler v0.86.51

- Fixes **Bug #081** by encoding AdapterProtocol guard/deferral and stopped
  state semantics in a minimal V3 TypeScript fixture
  (`adapter_protocol_minimal.frm`) and ensuring codegen preserves those rules.
- Adds a shared adapter semantics test harness under
  `framepiler_test_env/adapter_protocol` with a pinned TypeScript toolchain
  and a Node-based smoke test (`npm run smoke`) that exercises start,
  runtimeConnected, guarded pre-ready commands, ready/continue, and stopped
  behavior.
- Keeps the V3 TypeScript CLI fixtures green for `language_specific_typescript_v3_cli`,
  including the new `adapter_protocol_minimal` fixture under `@tsc-compile`.

