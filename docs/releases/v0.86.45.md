# Frame Transpiler v0.86.45

- Finalizes the fix for **Bug #075** (TypeScript transition temp redeclare) by:
  - Emitting per-state transition temporaries (`__frameNextCompartment_<StateName>`) instead of a single shared identifier, ensuring no TS2451 redeclaration even across multiple `switch (c.state)` cases in a single handler.
- `run_validate.sh` for Bug #075 now passes cleanly with `framec 0.86.45`, and the V3 TS CLI regression fixture `redeclare_next_compartment.frm` continues to compile under `tsc` without errors.

