# Frame Configuration

Centralized references for project configuration and native import integration.

- `frame_toml_guide.md` — project configuration (frame.toml), resolution order, examples.
- `fid_config.md` — settings that control FID generation/consumption (lock policy, cache paths, vendor directories), and how they interact with `framec fid import`.

See also:
- `../frame_interface_definition/native_imports_and_fid.md` — FID manifest format and `.fid` grammar.
