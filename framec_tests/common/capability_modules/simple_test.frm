# Simple test module to check Frame module compilation

module SimpleTest {
    fn add(a, b) {
        return a + b
    }
    
    fn multiply(a, b) {
        return a * b
    }
    
    fn greet(name) {
        return "Hello, " + name
    }
}