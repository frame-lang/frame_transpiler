fn main() {
    # Test basic constructs to validate source mapping coverage
    
    # Variable assignments
    var x = 42
    var name = "test"
    
    # Collections
    var list_val = [1, 2, 3]
    var dict_val = {"key": "value"}
    var set_val = {1, 2, 3}
    var tuple_val = (1, 2)
    
    # Expressions
    var result = x + 10
    var negated = -x
    var comparison = x > 10
    
    # Function calls
    print("Hello World")
    print(list_val)
    print(dict_val)
    print(set_val)
    print(tuple_val)
    
    # Conditional
    if x > 0 {
        print("Positive")
    }
}