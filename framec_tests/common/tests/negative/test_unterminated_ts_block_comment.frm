@target typescript

system TsUnterminatedBlockComment {
  actions:
    doThing() {
      /* ` a block comment that never closes
      const x = 1;
    }

  interface:
  machine:
    $S {
      e -> $S
    }
  domain:
}
