# Test all collection constructors
fn main() {
    # These generate but may not work correctly
    var l = list()
    var d = dict()
    var s = set()
    var t = tuple()
    
    print("Empty list: " + str(l))
    print("Empty dict: " + str(d))
    print("Empty set: " + str(s))
    print("Empty tuple: " + str(t))
}