// EXPECTED_OUTPUT: Enter event with empty paramsInterface method with empty params calledTest completed successfully!
fn main() {
    var service = ValidationService()
}

system ValidationService {

    interface:
        interface_method()

    machine:

    $Start {
        $>() {
            print("Enter event with empty params")
            -> $Done
        }
    }

    $Done {
        interface_method() {
            print("Interface method with empty params called")
            -> $Finished
        }
    }

    $Finished {
        $>() {
            print("Test completed successfully!")
        }
    }
}