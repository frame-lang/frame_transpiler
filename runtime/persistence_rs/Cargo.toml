[package]
name = "frame_persistence_rs"
version = "0.86.63"
edition = "2021"
license = "MIT"
description = "Persistence helpers for Frame Rust systems (Stage 15)."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

