# Frame Transpiler Version Configuration
# This file controls the version across all components

[version]
# Semantic version (major.minor.patch)
major = 0
minor = 86
patch = 72

# Full version string (generated from components above)
# Used for CLI --version, generated code comments, etc.
full = "0.86.72"

# Release metadata (optional)
[metadata]
release_date = "2025-12-13"
release_type = "bug-fix"  # major, minor, bug-fix, hotfix
build_number = 63
